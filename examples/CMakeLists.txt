# Convenience target that builds all examples.
# Example executable targets should be added to the 'examples' target as dependencies.
add_custom_target(examples DEPENDS)

add_subdirectory(Kd_density)
